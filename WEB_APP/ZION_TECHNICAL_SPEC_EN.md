# ZION SYSTEM: TECHNICAL SPECIFICATION & ARCHITECTURE
**Version:** 2.1 (Resilience Edition)
**Status:** Sovereign · Decentralized · Zero-Footprint

---

## 1. MISSION & IDENTITY
ZION is a decentralized digital vault designed for extreme resilience in high-risk environments. It operates as a **Hybrid Monolith**: a complete ecosystem contained within single HTML files, requiring no server-side execution, no databases, and no external dependencies once loaded.

---

## 2. CORE ARCHITECTURE (THE HYBRID MONOLITH)
ZION follows a strict **Vanilla JS Purist** philosophy to ensure compatibility across decades and devices.

### 2.1. Logic Layers
The system is structured into seven logical sections within each file:
1.  **CONFIG**: Immutable constants and operational parameters.
2.  **UTILS**: Pure helper functions (Crypto, Encoding, DOM helpers).
3.  **STATE**: Native state management (Observer pattern/FSM).
4.  **STORAGE**: Dual-layer persistence (IndexedDB for data, LocalStorage for config).
5.  **DOMAIN**: Core business logic (Encryption, Steganography, Protocols).
6.  **UI BINDINGS**: Reactive DOM manipulation without frameworks.
7.  **BOOTSTRAP**: Secure initialization and integrity checks.

---

## 3. THE ZION TRIAD (VERSIONS)

### 3.1. ZION WEB (THE VAULT)
*   **Role:** Primary operations hub.
*   **Capabilities:** Full encryption/decryption suite, Dead Man Switch, multi-file management, and Arka Steganography module.
*   **Storage:** Uses the **Omnibus Architecture** to store multiple documents inside a single browser environment securely.

### 3.2. ZION CORE (THE PRINTER)
*   **Role:** Distribution and replication node.
*   **Mechanism:** Embeds a compressed version of the ZION Lite template. When a PDF "Cartridge" is loaded, the Core "prints" a new ZION Lite file.
*   **Usage:** Used to generate localized survival kits from any device.

### 3.3. ZION LITE (THE CARTRIDGE)
*   **Role:** Ultra-portable, single-purpose document.
*   **Optimization:** Uses **GZIP + Base64** payloads. A 15MB PDF is reduced to ~2MB and embedded directly into the HTML.
*   **Portability:** Works 100% offline. Zero external requests. Includes a built-in PDF.js worker.

---

## 4. SECURITY PROTOCOLS

### 4.1. Panic Protocol (NUKE)
*   **Trigger:** Manual button or security breach.
*   **Action:** Immediate memory shredding of encryption keys and DOM destruction. The interface is replaced by a "Black Hole" state.
*   **Technical:** Clears `window` state and triggers `localStorage.clear()` + `indexedDB.deleteDatabase()`.

### 4.2. Dead Man Switch (DMS)
*   **Logic:** Inactivity timer (configurable).
*   **Behavior:** If no user interaction is detected within the threshold, the system assumes the user is compromised and triggers the Panic Protocol.

### 4.3. Zero-Footprint Deployment
*   **Privacy:** No cookies, no trackers, no telemetrics.
*   **Integrity:** All external scripts (CDNs) are loaded with **Subresource Integrity (SRI)** hashes.

---

## 5. BUILD SYSTEM & PIPELINE

The build system (Node.js) is the "Forge" of Zion.
1.  **GZIP Level 9:** Maximum compression of binary assets (PDFs/Workers).
2.  **Base64 Injection:** Transforming binaries into safe UTF-8 strings.
3.  **Placeholder Replacement:** Dynamic injection of payloads into `[[PAYLOAD_HERE]]` tags.
4.  **Localization:** Automatic injection of 24 language dictionaries into the translation engine.

---

## 6. IDEAL OPERATIONAL ENVIRONMENT (EFFECTIVENESS)

For maximum effectiveness and security, ZION should be used in the following "Ideal Environments":

### 6.1. Tactical Environment (High Risk)
*   **Device:** Air-gapped (no internet) machine or "Burner" device.
*   **Browser:** Tor Browser (for anonymity) or Brave/LibreWolf (for strict privacy).
*   **Distribution:** Hand-to-hand via encrypted USB or hidden inside Arka Images.

### 6.2. Resilient Environment (Censorship)
*   **Network:** Accessing via IPFS or Arweave Gateways.
*   **Redundancy:** If one gateway is blocked, the user probes a decentralized list of alternative nodes.

### 6.3. Everyday Use (Privacy)
*   **Storage:** Local execution from a `.html` file saved on the desktop.
*   **Sincronización:** Using the "Propagate" feature to sync Omnibus data between trusted devices via encrypted fragments.

---

## 7. CAPABILITIES SUMMARY
*   **Offline-First:** No internet needed after the first load.
*   **Sovereign Data:** You own the keys, you own the storage.
*   **Localized:** Native support for 24 languages, including RTL (Arabic/Persian) and complex scripts (Amharic/Burmese).
*   **Steganographic:** Ability to hide entire systems inside innocent-looking images.

---
*Document generated by the Architect for Zion Resilience.*
